pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/kriti28aug/wfa-jenkins.git'
            }
            
        }
    
    }
}
